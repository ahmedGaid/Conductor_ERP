# SESSION 21 — Acceptance, Regression & Sign-off
# Files: none new (fix-only session) — PARITY.md, PERF.md, RELEASE.md finalized

**Objective:** prove, end-to-end and in writing, that Conductor ARP mobile is ONE PRODUCT with
the web app and is launch-ready. This session builds nothing; it verifies everything, fixes
what verification finds, and signs off. Run on: real low-end Android + real iPhone + one tablet,
production-candidate builds, against a staging backend seeded like a real company.

---

## Full acceptance checklist

**Identity & parity**
- [ ] PARITY.md: every row done or deferred-with-reason; zero blanks; deferred list read aloud
      and each deferral still justified TODAY (not just when written)
- [ ] Side-by-side hour: one operator does the same 10 tasks on web and phone — same terms
      (Identity System §6 spot-check: one Arabic word per concept, both surfaces), same
      statuses, same numbers to the piaster, same permission walls
- [ ] A restricted user and an admin each tour the app — nothing appears on mobile that web's
      RBAC hides, nothing missing that it grants

**The golden flows (Arabic first, then English; each also on tablet)**
- [ ] Field sale: scan items → invoice create → submit → PDF share to WhatsApp → payment
      recorded — phone only, start to finish
- [ ] Taxi approvals: push arrives → killed-app tap → biometric → approve with undo →
      web reflects — under 30 seconds
- [ ] Storekeeper: continuous-scan receiving against a PO, one unknown barcode handled
- [ ] Accountant read: dashboard → drill to ledger → source doc → trial balance matches web
- [ ] AI flagship: WhatsApp PDF → share to assistant → extract → detour (create supplier,
      prefilled) → auto-resume → confirmed draft invoice → visible on web with clean audit trail
- [ ] Offline day: airplane mode — browse cached modules, create 2 records + 1 photo attachment,
      attempt an invoice (honest block) → reconnect → everything syncs exactly once, Outbox ends
      empty and says so

**Quality bars (re-verified, not remembered)**
- [ ] PERF.md budgets re-measured on the release candidate — all green or re-justified
- [ ] Session 17 accessibility spot-checks: TalkBack approval flow, 200% type on invoice create
- [ ] Dark mode + light mode sweep of every screen family; RTL/LTR sweep; reduced-motion sweep
- [ ] Security spot-checks: sign-out wipe (filesystem inspected), MITM refusal, revoked-device
      lockout — the session 18 trio, on release builds

## Regression checklist (the rest of the product)

- [ ] Full backend suite green (`pytest`); web gates green (`check-i18n-parity`, `tsc`, gate03)
- [ ] Web app manual tour: login, dashboard, an invoice, the assistant panel — mobile's backend
      additions changed nothing for web users
- [ ] Django admin loads; migrations linear; no orphaned tables from the plan's iterations
- [ ] Mobile gates green (`gate_mobile`); all Maestro flows green on both platforms; unit suite
      green; crash dashboard clean over the beta-bake window

## Micro-polish pass (apply what's missing, skip what exists)

- [ ] Every toast/dialog copy read aloud in Arabic — anything that sounds like a machine gets
      rewritten via the brand lexicon
- [ ] Empty states tour: all designed, none accidental; loading skeletons match final layouts
      (no layout jump when data lands)
- [ ] Haptics audit: destructive/confirm only — remove any buzz that crept elsewhere
- [ ] Icon gallery vs web icon set: same optical weight; badge counts align optically in RTL
- [ ] App icon, splash, store screenshots against the release build (not stale mockups)

## Brand-feel checklist

Run `conductor-brand`'s full checklist over: sign-in, dashboard, invoice detail, approvals
inbox, assistant conversation, scanner, Outbox. The question for each: **"would Linear ship
this?"** — anything answering no becomes a fix in THIS session or a written, owned exception.

## Sign-off block

Write at the bottom of PARITY.md:
- What was built: (summarize per phase, with commit range)
- What was deliberately NOT built: (deferred rows + the online-only financial-posting boundary
  + scope-freeze modules)
- Budgets at sign-off: (cold start, crash-free %, suite times)
- Signed: date, app version, backend migration state, who verified
- Then: update the `erp-status` skill anchor, rename this file `_done`, and hand RELEASE.md its
  first production release.

**Launch gate:** every box above checked → submit/rollout per RELEASE.md. Any box unchecked →
this session repeats after fixes. There is no "mostly done" — that is the difference between
this plan finishing and the app being launchable the next day.

*Generated by ag-plan skill.*
