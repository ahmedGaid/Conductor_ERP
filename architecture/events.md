# Domain Events

> Catalog of domain events: name, publisher module, payload shape, subscribers.
> Events are the sanctioned cross-module channel. A failing subscriber never breaks the publisher
> (see `erp/core/events.py`).

| Event | Publisher | Payload | Subscribers |
|---|---|---|---|
| _(none yet — populated as modules emit events, e.g. InvoiceCreated, InventoryUpdated, WorkflowStarted)_ | | | |
