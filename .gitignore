# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Django
*.sqlite3
/staticfiles/
/mediafiles/
/storage/

# Env / secrets
.env
.env.*
!.env.example
# prod env TEMPLATE (no secrets) — must be tracked
!deploy/.env.prod.example
# Docker env TEMPLATE (no secrets) — must be tracked
!.env.docker.example

# Deployment runtime artifacts (Phase 11)
deploy/logs/

# Node / frontend
node_modules/
apps/web/dist/
apps/web/.vite/

# Playwright E2E (twenty-harvest FILE_04) — saved logins + run artifacts, not source.
# Paths below are relative to CWD (apps/web, where `npm run e2e` runs), NOT the config file's own
# directory — Playwright resolves `storageState` and reporter output that way, unlike `outputDir`.
apps/web/.auth/
apps/web/playwright-report/
apps/web/blob-report/
apps/web/e2e/test-results/

# Logs / runtime
*.log
/logs/
/run/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/

# Build artifacts / specs archive
files.zip
temp/

# Local working artifacts (not project source)
project_context.md
erp_questionnaire_v4.html

# Eval scoreboards are generated per run (T1.6) — recordings are checked in, results are not
/erp/assistant/evals/results/*.json

# Weekly AI ops reports are generated per run (T1.9) — not project source
/Docs/ops/*.md

# CodeGraph index cache (local tooling)
.codegraph/

# Start-Conductor.cmd runtime log (local launcher)
conductor-dev.log
