# Source file for the pinned lockfile (requirements.txt), compiled via pip-compile
# (post-handover-v1_1 FILE_02). Edit THIS file for version-range changes, then re-run:
#   pip-compile requirements.in -o requirements.txt
#
# Core backend stack — pinned to majors; Stage 0 foundation.
Django>=5.1,<5.2
djangorestframework>=3.15,<3.16
django-environ>=0.14.0,<0.15
psycopg[binary]>=3.2,<3.3
celery>=5.4,<5.5
redis>=5.0,<6.0
pydantic>=2.8,<3.0
argon2-cffi>=23.1,<26.0
djangorestframework-simplejwt>=5.3,<5.4
pyotp>=2.9,<3.0                # TOTP 2FA (Stage 1)
django-cors-headers>=4.4,<5.0
python-json-logger>=2.0        # structured JSON logging
openpyxl>=3.1,<3.2             # XLSX report export (Stage 6b; pure-python, offline-safe)
whitenoise>=6.7,<7.0           # serve the built React bundle + Django static behind Django (Phase 11)
waitress>=3.0,<4.0             # pure-python WSGI server (Windows-friendly; gunicorn is POSIX-only)
# JSON-logic engine for workflow edges/script node is added in Stage 2 (pinned then).
anthropic>=0.92,<1.0           # AI assistant (plan session 02); optional at runtime — off without an API key
google-genai>=1.0,<2.0         # AI assistant alt provider (client request 2026-07-02): Gemini via GEMINI_API_KEY

# Dev / test
pytest>=8.3
pytest-django>=4.9
pytest-cov>=5.0         # coverage reporting (post-handover-v1_1 FILE_03)
mypy>=1.11
ruff>=0.6
bandit>=1.7,<2.0        # security lint (post-handover-v1_1 FILE_01)
pip-tools               # compiles this file into requirements.txt
