"""licensing — perpetual license keys for customer-hosted installs (license-key plan).

FILE_01: the signed key format + offline verifier (``core``, ``verify``). Nothing is enforced
until ``LICENSE_MODE=enforce`` and the later plan files land; ``off`` (the default) changes nothing.
"""
