"""Domain events published by the e-invoicing module."""
from __future__ import annotations

EINVOICE_RECORDED = "einvoice.Recorded"
EINVOICE_SUBMITTED = "einvoice.Submitted"
EINVOICE_VALIDATED = "einvoice.Validated"
EINVOICE_REJECTED = "einvoice.Rejected"
