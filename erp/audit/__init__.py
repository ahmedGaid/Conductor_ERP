"""audit — immutable, append-only audit trail for every business action.

Stage 1 wires writes into services and exposes a read API. The model is defined now so the
table exists from the first migration.
"""
